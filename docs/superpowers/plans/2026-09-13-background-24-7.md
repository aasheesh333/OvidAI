# 24/7 Background Operation — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL:
> superpowers:subagent-driven-development (recommended) or
> superpowers:executing-plans. Checkbox (`- [ ]`) syntax.

**Goal:** Harden the foreground service, restart after boot, request battery
exemption, and add an in-app stop — best-effort always-on with honest limits.

**Spec:** `docs/superpowers/specs/2026-09-13-background-24-7-design.md`

## Global Constraints

- No DSH references in `lib/`/`test/`.
- RED test first; full `flutter test` + `flutter analyze` green.
- Flutter binary `/root/flutter/bin/flutter`.
- Device rows `NOT EXECUTED`.

---

### Task 1: FGS hardening + Android 14 timeout
**Files:** `AgentForegroundService.kt`, `MainActivity.kt`, `AndroidManifest.xml`
- [ ] RED: service start/update/stop intent sequence.
- [ ] Harden `dataSync` FGS; handle timeout re-start.
- [ ] GREEN.

### Task 2: Boot restart receiver
**Files:** `AndroidManifest.xml`, new `BootReceiver.kt`, `agent_notification_service.dart`
- [ ] RED: manifest pin for `BOOT_COMPLETED` + keep-alive gate.
- [ ] Implement.
- [ ] GREEN.

### Task 3: Battery exemption
**Files:** `MainActivity.kt`, `settings_screen.dart`, `state.dart`
- [ ] Request exemption + OEM autostart deep-link; disclose.
- [ ] Pin the intent.

### Task 4: In-app stop
**Files:** `settings_screen.dart` / `health_screen.dart`, `agent_notification_service.dart`
- [ ] RED: in-app Stop/Exit mirrors notification.
- [ ] Implement.
- [ ] GREEN.

### Task 5: Verify + audit
- [ ] Full `flutter test` + `flutter analyze`; `git diff --check`.
- [ ] Audit `docs/superpowers/audits/2026-09-13-background-24-7.md`.
